<Window x:Class="EventLogCollector.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:EventLogCollector.Gui.Views"
        mc:Ignorable="d"
        Title="Event Log Collector" Height="700" Width="1100">
    <Window.Resources>
        <!-- Strips the TabControl down to just its content presenter — no tab strip is ever
             rendered, so navigation is driven entirely by the sidebar. -->
        <Style x:Key="HiddenTabControl" TargetType="TabControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <!-- Zero-height but real ItemsHost: keeps TabItem containers (and
                                 therefore their content's automation tree) alive without ever
                                 rendering a visible tab strip. -->
                            <TabPanel Grid.Row="0" IsItemsHost="True" Height="0" ClipToBounds="True" />
                            <ContentPresenter x:Name="PART_SelectedContentHost" Grid.Row="1" ContentSource="SelectedContent" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="20,14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#FF3B6EA5" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#FF2E4B6B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FF1E2A38">
            <ListBox Background="Transparent" BorderThickness="0"
                     ItemContainerStyle="{StaticResource SidebarItem}"
                     SelectedIndex="{Binding SelectedPageIndex, Mode=TwoWay}"
                     Foreground="White" FontSize="15" Margin="0,16,0,0">
                <ListBoxItem Content="Recording" />
                <ListBoxItem Content="Logs" />
                <ListBoxItem Content="Correlated Logs" />
            </ListBox>
        </Border>

        <TabControl Grid.Column="1"
                    Style="{StaticResource HiddenTabControl}"
                    SelectedIndex="{Binding SelectedPageIndex, Mode=TwoWay}"
                    BorderThickness="0" Background="Transparent">
            <TabItem>
                <views:RecordingPageView DataContext="{Binding Recording}" />
            </TabItem>
            <TabItem>
                <views:LiveLogsPageView DataContext="{Binding LiveLogs}" />
            </TabItem>
            <TabItem>
                <views:CorrelatedLogsPageView DataContext="{Binding CorrelatedLogs}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
